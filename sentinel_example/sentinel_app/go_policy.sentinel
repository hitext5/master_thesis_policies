import "plug"
import "electronic_device"

enough_power = rule {
    plug_rated_power >= electronic_device_work_power
}

enough_slots = rule {
    plug_slots >= 1
}

correct_action = rule {
    input_act == "plug_in"
}

main = rule {
    enough_power and enough_slots and correct_action
}